# 校园二手交易平台 (Campus Market)

面向高校学生的二手商品交易平台，包含 Web 网站与微信小程序两端，各自覆盖用户端与管理端。前后端一体，开箱即用。

> 学习/实践用途项目。隐私保护设计（联系方式加密 + 盲索引）与信用分体系是核心亮点。

## 功能特性

**用户端**
- 商品发布/浏览/搜索/筛选（分类、价格、成色、多因子热门排序）
- 收藏、浏览历史、评价（1-5 星，信用分联动）
- 预订交易流：预订 → 取消/完成，订单状态机双表联动
- 私聊（会话 + 5 秒轮询增量拉取，可平滑升级 WebSocket）
- 通知中心（订单/评价/举报/系统通知，含未读角标）
- 信用分体系（0-100）：交易加分、违规扣分，低于 10 分自动冻结账号
- 举报系统（商品/用户，处理结果联动信用分）

**管理端**
- 仪表盘：11 项运营指标
- 用户管理：搜索/封禁/启用/重置密码/调整信用分
- 商品管理：搜索/详情（解密联系方式）/强制下架/重新上架审核
- 订单、举报（警告/下架/封禁/驳回 + 恶意举报标记）、分类管理

## 系统架构

```
┌────────────────────────────────────────────────┐
│  Web 网站（原生 HTML/CSS/JS，24 页）             │
│  微信小程序（原生 WXML/WXSS/JS，22 页）          │
└────────────────────┬───────────────────────────┘
                     │ HTTP / JSON  {code, msg, data}
┌────────────────────▼───────────────────────────┐
│  Spring Boot 2.7 单体应用（:8080）              │
│  Controller → Service → Mapper                 │
│  JWT 全局鉴权 · DFA 敏感词 · @Scheduled 定时任务  │
└────────────────────┬───────────────────────────┘
                     │ MyBatis-Plus
┌────────────────────▼───────────────────────────┐
│  MySQL 8.0（15 张表，utf8mb4）                  │
└────────────────────────────────────────────────┘
```

## 技术栈

| 层 | 技术 |
|----|------|
| 后端 | Spring Boot 2.7.18 · MyBatis-Plus 3.5.5 · JJWT 0.11.5 · Hutool · Knife4j |
| 数据库 | MySQL 8.0（15 张表）：全局逻辑删除 · 乐观锁 `@Version` · 字段自动填充 · HMAC 盲索引 |
| Web 前端 | 原生 HTML5/CSS3/JS（无框架、无构建工具）· 自绘 SVG 图标库 · CSS 变量设计系统 · 移动端适配 |
| 小程序 | 原生 WXML/WXSS/JS（无 uni-app）· 自定义 tabBar · 本地图标组件 · rpx 适配 |
| 安全 | BCrypt 密码 · AES-GCM 加密（联系方式，随机 IV）· HMAC-SHA256 盲索引 · DFA 敏感词 · JWT（仅 Authorization 头传递） |
| 定时任务 | Spring `@Scheduled`：预订超时释放 · 联系不上超时 · 系统默认好评 · 超期自动下架 |
| 测试 | Bash + curl 接口回归脚本（66 项断言，自动创建并清理测试数据） |
| 文档 | Knife4j（`/doc.html` 在线接口文档） |

## 设计亮点

| 模块 | 实现方式 |
|------|---------|
| 联系方式加密 | 手机号 AES-GCM 密文存储（每次随机 IV）+ HMAC-SHA256 盲索引用于等值查询，数据库不落明文 |
| 信用分体系 | 统一变更入口 + 分值 clamp + 变更日志只增不改 + 乐观锁；低于 10 分自动冻结（下架在售商品并通知相关买家），分值回升自动解冻 |
| 订单状态机 | 商品与订单双表联动；取消恢复在售、超时释放、联系不上幂等提交（防重置计时器） |
| 强制下架审核 | 管理员强制下架打标记 → 卖家修改后提交审核 → 管理员通过/驳回，可反复流转 |
| 热门排序 | 多因子 + 时间衰减：近 7 天浏览×1 + 收藏×5 + 新品期×25，配合浏览日统计表 |
| 站内私聊 | 会话/消息两张表 + 5 秒增量轮询（afterId），表结构可平滑升级 WebSocket |

## 快速开始

### 环境要求

- JDK 17
- Maven 3.6+
- MySQL 8.0

### 1. 初始化数据库

```bash
mysql -uroot -p < database/campus_market_init.sql
```

### 2. 配置

`backend/src/main/resources/application.yml` 中可通过环境变量覆盖敏感配置（默认值仅供本地开发）：

| 环境变量 | 说明 | 默认值（仅本地开发用） |
|---------|------|----------------------|
| `DB_USERNAME` / `DB_PASSWORD` | 数据库账号密码 | root / 123456 |
| `JWT_SECRET` | JWT 签名密钥 | 内置测试值 |
| `AES_KEY` | 手机号 AES-GCM 加密密钥 | 内置测试值 |
| `HMAC_KEY` | 手机号盲索引 HMAC 密钥 | 内置测试值 |

> ⚠️ **生产部署必须通过环境变量更换全部密钥**，否则持有源码者即可解密全部用户联系方式。

### 3. 运行

```bash
cd backend
mvn spring-boot:run
```

- 用户端首页：http://localhost:8080/
- 管理端：http://localhost:8080/admin/index.html（管理员账号：admin / admin123，登录后请尽快修改）
- API 文档：http://localhost:8080/doc.html

### 4. 运行小程序

微信开发者工具 → 导入 `miniprogram/` 目录（appid 可用测试号）→ 本地设置中勾选「不校验合法域名」→ 确保后端已在 8080 端口运行。后端地址在 `miniprogram/utils/request.js` 的 `BASE` 常量中配置。

### 5. 回归测试

```bash
bash scripts/api_test/run_all.sh    # 66 项接口断言测试（自动创建并清理测试数据）
```

## 安全设计

- **联系方式加密**：手机号 AES-GCM 加密存储（每次随机 IV），`phone_hash` HMAC-SHA256 盲索引用于等值查询，数据库不落明文
- **订单联系方式快照**：预订时买家选填手机/QQ/微信（至少一种），仅该订单卖家可见；买家隐私由买家自主决定
- **会话安全**：JWT 仅从 Authorization 头传递；拦截器每次请求校验用户状态（封禁即时生效）；登录失败 5 次锁定 15 分钟；用户不存在与密码错误返回统一提示（防账号枚举）
- **输入安全**：所有用户输入前端 escapeHtml 转义；标题/描述/评价/私聊消息 DFA 敏感词过滤

## 核心业务规则

- 订单状态机：商品 在售→预订中→已售出（取消恢复）；订单 待交易→已完成/买家取消/卖家取消/超时取消
- 信用分：完成交易 +2、好评 +1、买家取消 -3、差评 -2、举报核实 -10；<10 自动冻结（含下架在售商品、通知进行中订单买家），加分回升自动解冻
- 强制下架审核：被管理员下架的商品，卖家需修改后提交审核，审核通过方可重新上架
- 热门排序：近 7 天浏览×1 + 收藏×5 + 新品期(≤3天)×25

## 目录结构

```
├── backend/                        # Spring Boot 应用（后端 + Web 前端静态资源）
│   └── src/main/resources/static/  # Web 前端：用户端 17 页 + 管理端 7 页
├── miniprogram/                    # 微信小程序（原生）：用户端 16 页 + 管理端 6 页
├── database/
│   └── campus_market_init.sql      # 建库建表 + 初始化数据（15 张表）
└── scripts/api_test/               # 接口回归测试（66 项断言）
```

## 免责声明

本项目为学习/教学用途，不构成商业产品。二手交易涉及资金往来，请谨慎使用。
