# 校园二手交易平台 — 产品功能文档 v11.0

> **文档版本**：v11.0  
> **生成日期**：2026-07-13  
> **文档状态**：评审中  
> **目标用户**：齐鲁工业大学在校学生  
> **项目周期**：7周开发 + 1周灰度测试 + 上线运营  
> **产品形态**：微信小程序 + Web端（uni-app 双端编译，同一套代码）

---

## 一、产品概述

### 1.1 产品定位

**校园二手交易平台**是一款面向在校大学生的校内闲置物品交易工具，同时提供**微信小程序**和 **Web 网页端**两个访问入口。聚焦实物二手交易，通过学号实名认证建立信任基础，以线下面对面交易规避资金风险，用信用评价体系约束用户行为。小程序借助微信生态实现快速传播和便捷体验，Web 端覆盖非微信场景（如电脑浏览、QQ分享打开），两端共享同一后端，用户数据互通。

### 1.2 竞品分析

| 竞品 | 优势 | 劣势 |
|------|------|------|
| 闲鱼 | 用户基数大、品类全 | 非校园专属，信任成本高；需快递/支付 |
| 转转 | 验机服务、品类聚焦 | 同样非校园场景；交易流程重 |
| 校内QQ群/微信群 | 零开发成本、即时沟通 | 信息混乱难检索；无信任机制；易刷屏 |
| 校园墙/表白墙 | 触达率高、传播快 | 无商品结构化信息；无法搜索和筛选 |

**本平台差异化**：微信小程序形态（无需下载安装、扫码即用） → 微信内一键分享班级群/好友 → 微信授权登录 + 学号实名 → 线下当面验货 → 零资金风险 → 订阅消息触达 → 轻量工具型产品。

### 1.3 目标用户画像

| 用户角色 | 典型场景 | 核心诉求 |
|---------|---------|---------|
| **教材买家**（大一-大三） | 每学期初找教材，不想原价买新书 | 找同专业学长学姐的二手教材，价格便宜 |
| **毕业生卖家**（大四） | 离校前处理大量闲置 | 快速出手，不在乎价格，图省事 |
| **数码爱好者**（大二-大四） | 升级换代出旧设备 | 买家能当面验货，不用担心被坑 |
| **生活达人**（各年级） | 台灯、收纳、小家电等日用品 | 比网购便宜，即买即用 |
| **书商/校外用户**（需防范） | 伪装学生批量出售盗版/劣质商品 | 需要学号验证机制拦截 |

### 1.4 功能边界

| 功能 | V1.0（本次） | V1.5（预留） | V2.0（远期） |
|------|:-----------:|:-----------:|:-----------:|
| 实物二手交易 | ✅ | — | — |
| 微信授权登录（小程序） | ✅ | — | — |
| 手机号+密码登录（Web） | ✅ | — | — |
| 学号实名认证 | ✅ | — | — |
| 商品发布/浏览/搜索 | ✅ | — | — |
| 收藏/预订 | ✅ | — | — |
| 信用评价系统 | ✅ | — | — |
| 举报/违规处理 | ✅ | — | — |
| 后台管理（Web） | ✅ | — | — |
| 小程序分享（转发好友/群） | ✅ | — | — |
| 微信订阅消息通知（小程序） | ✅ | — | — |
| 站内即时通讯 | ❌（用微信对话过渡） | ✅ | — |
| 求购/许愿单 | ❌ | ✅ | — |
| 图片内容审核 | ❌ | ✅（接入微信内容安全API）| — |
| 学币/积分系统 | ❌ | ❌ | ✅ |
| 电子资料交易 | ❌ | ❌ | ✅ |
| 在线支付/担保交易 | ❌ | ❌ | ❌（坚持线下） |
| 快递/物流 | ❌ | ❌ | ❌（坚持校内面交） |

**原则**：V1.0只做交易信息撮合，不碰钱不碰物流，最大程度降低系统复杂度与法律风险。

---

## 二、术语定义

| 术语 | 定义 |
|------|------|
| MVP | Minimum Viable Product，最小可行产品 |
| 学号认证 | 用户注册时校验学号为齐鲁工业大学在籍学生 |
| 成色 | 商品新旧程度分级，详见§4.2.2 |
| 交易闭环 | 商品从"发布→预订→交易确认→互评"的完整生命周期 |
| 敏感词 | 违禁品、广告引流、色情、政治等违规词汇集合 |
| 预订 | 买家表达购买意向，锁定商品不被他人购买，24h内需完成交易或取消 |
| 预订释放 | 预订后24h内买家未确认交易，系统自动取消预订，商品恢复"在售" |
| 交易确认 | 买家线下验货付款后，在平台确认交易完成 |
| 信用分 | 基于交易完成率、好评率、违规记录计算的综合信誉分值（0-100） |
| 灰度发布 | 先对部分用户开放新功能，验证无问题后全量推送 |

---

## 三、核心业务流程

### 3.1 完整交易闭环

```
                    ┌──────────┐
                    │ 卖家发布  │
                    │   商品    │
                    └────┬─────┘
                         │ 即时上架（敏感词审核通过后）
                         ▼
                    ┌──────────┐
                    │ 买家浏览  │
                    │ 筛选/搜索 │
                    └────┬─────┘
                         │ 感兴趣
                         ▼
              ┌──────────────────┐
              │ 买家查看详情      │
              │ 查看卖家信用/评价  │
              └────┬───┬─────────┘
                   │   │
            收藏   │   │  预订
                   ▼   ▼
              ┌──────────────┐
              │ 商品"已预订"  │
              │ 双方可见对方  │
              │ 完整联系方式  │
              └──────┬───────┘
                     │ 线下约定时间地点
                     ▼
              ┌──────────────┐
              │  线下当面交易  │
              │  验货→付款    │
              └──────┬───────┘
                     │
         ┌───────────┼───────────────┐
         ▼           ▼               ▼
    ┌─────────┐ ┌─────────┐    ┌───────────┐
    │买家确认  │ │买家取消  │    │买家不做    │
    │交易完成  │ │预订      │    │任何操作    │
    └────┬────┘ └────┬────┘    └─────┬─────┘
         │           │               │
         ▼           ▼               ▼
    ┌─────────┐ ┌─────────┐    ┌───────────┐
    │状态→    │ │状态→    │    │24h后系统   │
    │已售出   │ │在售     │    │自动释放     │
    │双方可互评│ │         │    │商品回"在售" │
    │（7天内） │ │         │    │买家信用分-3 │
    └─────────┘ └─────────┘    └───────────┘
```

> **关键时间节点一览：**
> - 预订 → 24h未确认：自动释放
> - 交易确认 → 7天内互评：评价窗口
> - 7天未评 → 自动默认好评
> - 商品发布 → 30天未售出：自动下架（按最近上架时间计算）

### 3.2 关键流程说明

**1. 卖家发布商品 → 自动进入敏感词审核**
- 未命中敏感词 → 即时上架
- 命中敏感词 → 进入人工审核队列（24h内完成）

**2. 买家点击"我要预订"**
- 系统校验：买家是否已登录、信用分≥10、当前预订数<3、商品状态为"在售"
- 预订成功后：商品状态→"已预订"，生成交易订单，双方在订单详情页可见对方完整手机号和微信号
- 系统发送站内通知给卖家："您的商品《XXX》已被预订，请留意微信/电话联系"

**3. 预订后24h内**
- 买家通过微信/电话联系卖家，约定线下交易时间和地点
- 线下见面验货付款后，买家在平台点击"确认交易完成"
- 买家可随时点击"取消预订"（买家信用分-3）
- 卖家也可取消预订（如联系不上买家），需填写原因，不扣买家信用分

**4. 预订超时释放（24h）**
- 预订后24h内买家无任何操作 → 系统自动释放预订 → 商品恢复"在售" → 买家信用分-3
- 释放后该商品可被其他用户预订

**5. 卖家催确认**
- 预订后卖家和买家已联系上但买家忘记来平台确认 → 卖家可在"我的发布"中点击"提醒买家确认"（每天限1次），系统发送站内通知给买家

**6. 买家反馈联系不上卖家**
- 买家可在"我的预订"中点击"联系不上卖家"，系统发送通知给卖家（同一订单仅可提交一次，防止恶意重置计时器）
- 卖家可在"我的发布"中点击"已联系买家"响应，清除24h倒计时
- 卖家24h内未响应 → 订单自动取消 → 商品恢复"在售"

**7. 交易完成后的评价**
- 买家确认交易后 → 双方进入互评窗口（7天）
- 7天内未评价 → 系统自动默认好评

### 3.3 商品状态流转

```
                ┌─────────┐
         ┌─────→│  在售   │←─────────┐
         │      └────┬────┘          │
         │           │ 被预订        │ 释放/买家取消
         │           ▼              │
         │      ┌─────────┐         │
         │      │ 已预订   │─────────┘
         │      └────┬────┘
         │           │ 买家确认交易
         │           ▼
         │      ┌─────────┐
         │      │ 已售出   │
         │      └────┬────┘
         │           │ 卖家手动下架 / 30天超时
         │           ▼
         │      ┌─────────┐
         └──────│ 已下架   │←──卖家可重新上架
                └─────────┘
```

| 状态 | 说明 | 可见性 | 可执行操作 |
|------|------|--------|-----------|
| **在售** | 正常展示，可被浏览和预订 | 所有人可见 | 编辑信息 / 下架 |
| **已预订** | 有人预订，锁定中，24h倒计时 | 所有人可见，标注"已预订"，不可再预订 | 买家：确认交易 / 取消预订 / 反馈联系不上；卖家：提醒确认 / 取消预订 |
| **已售出** | 交易完成，不再展示 | 仅买卖双方在订单记录中可见 | 双方互评（7天内） |
| **已下架** | 卖家主动下架或30天自动下架 | 仅发布者可见 | 重新上架 / 删除 |

---

## 四、功能模块

### 4.1 用户系统

#### 4.1.1 注册

平台提供两套注册入口，最终绑定到同一个用户体系：

**小程序端（4步）：**
```
微信授权(获取openid+头像昵称) → 输入手机号+短信验证码 → 输入学号 → 设置密码 → 注册完成
```

**Web 端（4步）：**
```
输入手机号+短信验证码 → 设置密码 → 输入学号 → 设置昵称+头像 → 注册完成
```

> **设计说明**：小程序注册时微信头像/昵称自动带入，但仍需设定密码（Web端登录用）。手机号作为两端的唯一账号标识——小程序通过微信 openid 绑定手机号，Web 通过手机号+密码登录。同一手机号仅可注册一个账号，无论从小程序还是 Web 注册。

**学号验证规则（V1.0）：**

| 验证层级 | 方式 | 说明 |
|---------|------|------|
| 第一层 | 格式校验 | 12位数字，前缀 `20xx` 开头，符合齐鲁工业大学编码规则 |
| 第二层 | 手机号绑定 | 同一手机号仅可注册1个账号；同一学号仅可注册1个账号 |
| 第三层 | 白名单校验（V1.5实现） | V1.0暂用「格式校验+唯一性校验」；V1.5预置在校学生学号白名单（可向学校申请脱敏数据接口，或用学院+专业+年级的批量生成白名单进行模糊匹配） |
| 远期 | 教务系统API | 对接学校教务系统，实时校验学号+姓名 |

**各字段规则：**

| 字段 | 来源 | 规则 |
|------|------|------|
| 手机号 | 用户输入（小程序端可调用微信手机号快速验证组件预填） | 11位中国大陆手机号，发送短信验证码校验 |
| 密码 | 用户设置 | 8-20位，必须包含大写字母、小写字母和数字 |
| 学号 | 用户手动输入 | `20xx`开头的12位数字，注册后不可修改 |
| 微信 openid | 小程序端自动获取；Web端注册时为 NULL | 小程序唯一标识，后续可绑定 |
| 头像/昵称 | 小程序端来自微信授权；Web端用户自行设置 | 均可在个人中心修改 |

**防刷机制：**
- 发送短信验证码前，必须先完成图形验证码校验（滑块或点选）
- 同一 IP 每小时最多发送 10 条验证码
- 同一手机号每天最多发送 5 条验证码
- 后端记录所有验证码发送日志（含 IP、时间戳），便于事后审计

#### 4.1.2 登录

**小程序端 — 微信静默登录（默认）：**
```
小程序启动 → wx.login() 获取 code → 后端换取 openid → 查用户表
  ├── openid 已绑定 → 生成 token，自动登录
  └── openid 未绑定 → 跳转注册页
```

**Web 端 — 手机号+密码登录：**
```
输入手机号 + 密码 → 后端校验 → 生成 token → 进入首页
```

| 登录方式 | 适用端 | 说明 |
|---------|:--:|------|
| 微信静默登录 | 小程序 | 启动自动完成，无需用户操作 |
| 手机号 + 密码 | Web / 小程序 | Web 端主要方式；小程序端也可使用（如微信授权失败降级时） |

- 登录状态维持 30 天（Redis token + 前端本地缓存）
- 连续 3 次密码错误 → 强制弹出图形验证码
- 连续 5 次密码错误 → 锁定账号 15 分钟
- Web 端异地登录检测：IP 地域变化时发送短信提醒
- **双端互通**：用户在小程序注册后，可以用同一手机号在 Web 端登录，反之亦然

#### 4.1.3 个人中心

```
┌──────────────────────────────┐
│  头像（可点击修改）           │
│  昵称 ★★★★☆（信用分显示）     │
│  好评率 98%  |  已成交 12笔   │
├──────────────────────────────┤
│  ▸ 我的发布（在售3 | 已预订1 │
│               已售出8 | 下架2）│
│  ▸ 我的预订（待交易1）        │
│  ▸ 我的收藏（15件）           │
│  ▸ 我的评价（收到8条 | 发出5条）│
│  ▸ 浏览记录                  │
│  ▸ 消息通知（3条未读）        │
│  ▸ 账号设置（修改头像/昵称/密码）│
│  ▸ 账号注销                  │
│  ▸ 退出登录                  │
└──────────────────────────────┘
```

#### 4.1.4 账号注销

| 阶段 | 说明 |
|------|------|
| 前置条件 | ① 无在售商品 ② 无进行中的预订 ③ 无未处理的举报 |
| 申请 | 用户提交注销申请，通过微信授权验证身份（wx.getUserProfile 或手机号验证） |
| 冷静期 | 7天，期间可随时登录撤销注销 |
| 执行 | 7天后自动执行——匿名化保留已产生的交易记录和评价（关联到"已注销用户"），删除手机号/openid等隐私数据 |
| 合规 | 符合《个人信息保护法》第四十七条关于注销后删除个人信息的要求 |

**注意**：为保护交易对方的数据完整性，用户的交易记录和评价内容不会被物理删除，而是做匿名化脱敏处理（昵称→"已注销用户"，头像→默认头像）。

#### 4.1.5 联系方式展示策略

**核心原则**：联系方式仅对交易双方在预订期间可见，用于线下联络。商品发布时卖家填入的联系方式（手机号、微信号）存储于商品表，发布前用户可勾选"使用注册手机号"一键带入。

| 查看者身份 | 手机号 | 微信号 | QQ号 |
|-----------|--------|--------|------|
| 未登录 | 不可见 | 不可见 | 不可见 |
| 已登录（非交易相关） | 不可见 | 不可见 | 不可见 |
| 预订该商品的买家 | **完整显示** | **完整显示** | 完整显示 |
| 被预订商品的卖家 | **完整显示** | **完整显示** | 完整显示 |
| 交易完成后 | 双方各自仅能看到自己的 | 同前 | 同前 |

**说明**：
- 商品详情页不直接展示任何联系方式，仅显示"预订后可查看联系方式"
- 买家预订成功后，在订单详情页可见卖家的完整手机号和微信号，用于线下联络
- 卖家在订单详情页可见买家的完整手机号（来自用户注册信息）
- 交易完成后，订单详情页的联系方式保留7天（评价窗口期），之后隐藏

#### 4.1.6 信用分体系

| 维度 | 加分 | 扣分 |
|------|------|------|
| 交易完成 | +5分/笔 | — |
| 获得好评 | +2分/条 | — |
| 获得差评 | — | -10分/条 |
| 被举报核实 | — | -20分/次 |
| 恶意举报他人 | — | -15分/次 |
| 商品描述不符 | — | -10分/次 |
| 24h内确认交易 | +3分/笔 | — |
| 预订后超时释放 | — | -3分/次 |
| 预订后主动取消 | — | -3分/次 |

- 初始信用分：60分
- 信用分范围：0-100分
- 信用分 < 30分 → 限制发布（每日最多1件）
- 信用分 < 10分 → 冻结账号

### 4.2 商品模块

#### 4.2.1 商品分类

| 一级分类 | 二级分类 |
|---------|---------|
| 📚 教材书籍 | 公共课教材、专业课教材、考研资料、考公资料、四六级/考证、课外读物、其他书籍 |
| 💻 数码电子 | 手机、电脑/笔记本、平板、耳机/音箱、智能穿戴、配件/线材、其他数码 |
| 🏠 生活用品 | 台灯/照明、收纳/置物、床上用品、小家电、洗护/日用品、其他生活 |
| 👗 服装鞋包 | 男装、女装、鞋子、箱包、配饰 |
| ⚽ 运动健身 | 球类/球拍、健身器材、自行车/代步、户外装备、其他运动 |
| 🎮 兴趣娱乐 | 游戏/卡带、乐器、手办/模型、手工/文具、其他兴趣 |
| 📦 其他闲置 | 无法归类的闲置物品 |

#### 4.2.2 成色标准

| 成色 | 代号 | 定义 | 判定依据（辅助） | 建议折扣 |
|------|------|------|-----------------|---------|
| 全新未拆 | `NEW` | 未拆封/未使用，包装完整 | 可提供购买凭证/订单截图 | 原价7-9折 |
| 99新 | `LIKE_NEW` | 仅拆封/使用1-3次，无任何痕迹 | 可提供购买时间 | 原价5-7折 |
| 9成新 | `GOOD` | 正常使用痕迹，功能完好，轻微划痕 | 需上传瑕疵特写 | 原价3-5折 |
| 8成新 | `FAIR` | 明显使用痕迹，外观磨损，功能正常 | 需上传所有瑕疵图 | 原价2-3折 |
| 战损 | `POOR` | 外观严重磨损/破损，但核心功能正常 | 必须展示所有损坏 | 原价1-2折 |
| 故障品 | `DEFECTIVE` | 功能有缺陷/无法正常使用 | 必须如实描述故障 | 协商定价 |

**成色管控：**
- 发布时卖家选择成色，系统根据成色要求校验图片数量：
  - `NEW` / `LIKE_NEW`：至少1张图
  - `GOOD`：至少2张图（含1张瑕疵特写）
  - `FAIR` / `POOR`：至少3张图（含2张以上瑕疵特写）
  - `DEFECTIVE`：至少3张图 + 描述中必须说明具体故障
- 买家举报"描述不符"经核实后，记入卖家信用扣分
- 累计3次成色虚假标注 → 限制发布7天

#### 4.2.3 发布商品

**必填项：**

| 字段 | 规则 | 说明 |
|------|------|------|
| 商品标题 | 4-30字，不含敏感词 | 系统自动去首尾空格 |
| 一级分类 | 下拉选择 | 见§4.2.1 |
| 二级分类 | 联动一级分类 | 见§4.2.1 |
| 商品描述 | 10-500字，不含敏感词 | 支持换行，不支持富文本 |
| 商品价格 | 1-99999元整数 | 输入框实时校验 |
| 商品成色 | 选择成色等级 | 见§4.2.2 |
| 商品图片 | 1-9张，JPG/PNG | 单张≤5MB，自动前端压缩至≤2MB |
| 交易地点 | 预置校园地标选择 | 如：一食堂/二食堂/图书馆/XX宿舍楼/教学楼XX |
| 联系方式 | 手机号 / 微信号 | 至少填一项；支持"使用注册手机号"一键带入 |

**选填项：**

| 字段 | 说明 |
|------|------|
| 原价 | 原始购买价格（辅助买家判断性价比） |
| 购买时间 | 大概购买时间（辅助判断成色） |
| QQ号 | 额外联系方式 |

**发布规则：**
- 同一用户最多同时有 **20件"在售"** 商品（已预订/已售出/已下架不计入）
- 发布后即时进入敏感词审核：未命中→即时上架；命中→人工审核
- 同用户24h内最多发布5件（防批量灌水）

#### 4.2.4 图片规范

| 项目 | 要求 |
|------|------|
| 格式 | JPG、PNG、WEBP |
| 大小 | 单张≤5MB（前端自动压缩至≤2MB后上传） |
| 分辨率 | ≥ 480p，建议 ≥ 720p |
| 数量 | 1-9张（成色越低需要越多图，见§4.2.2） |
| 内容要求 | ① 首图须为商品整体照 ② 不得包含二维码/外站链接水印 ③ 不得使用与商品无关的图片 |
| 上传方式 | 支持相册选择/拍照上传，多图可拖拽排序 |

**图片处理流程（V1.0 本地存储）：**
```
用户选择图片 → 前端压缩(≤2MB) → uni.uploadFile 上传至后端
→ 后端校验(格式/大小/分辨率/魔术数字) → 生成缩略图(200×200)
→ 存储至本地磁盘 → 返回图片URL → 存入goods_image表
```

> **注意**：
> - 图片内容审核（涉黄涉暴检测）在 V1.0 不做，V1.5 接入微信内容安全API
> - 前端压缩：`uni.chooseImage({ sizeType: ['compressed'] })` 微信端自动压缩；Web 端使用 canvas 压缩
> - 双端统一通过后端上传，避免小程序和 Web 走不同通道的维护成本

**图片存储方案：**
- V1.0：本地存储，路径 `/data/images/{yyyy-MM}/{goods_id}/{序号}.jpg`
- Nginx 直接静态代理，配置跨域和缓存头（`Cache-Control: max-age=604800`）
- 缩略图与列表页加载优化：列表页请求 `_thumb` 后缀的缩略图版本
- 后续版本：迁移至阿里云 OSS + CDN 加速

#### 4.2.5 商品浏览

**列表页功能：**

| 功能 | 实现方式 |
|------|---------|
| 分类筛选 | 横向一级分类Tab + 下拉二级分类 |
| 价格筛选 | 快捷区间（0-20 / 20-50 / 50-100 / 100-200 / 200-500 / 500+）+ 自定义输入 |
| 成色筛选 | 多选（全新 / 99新 / 9成新 / 8成新 / 战损） |
| 排序 | 最新发布（默认）、价格↑、价格↓、浏览量、信用分高优先 |
| 分页 | 每页20条，滚动无限加载（移动端）/ 分页器（PC端） |

**商品卡片信息：**

```
┌─────────────────────────┐
│  [首图缩略图]           │
│  ¥30      99新          │
│  考研数学复习全书        │
│  一食堂交易  |  3小时前  │
│  ★ 98%好评  |  12人浏览  │
└─────────────────────────┘
```

**详情页结构：**

```
[图片轮播区]  ← 点击可全屏查看，支持左右滑动
[基本信息] 标题 / 价格 / 成色 / 原价（选填）/ 发布时间
[商品描述] 纯文本
[交易信息] 交易地点 / 购买时间（选填）
[卖家信息] 头像 / 昵称 / 信用分 / 好评率 / 在售数 / 已成交数
[评价区]   卖家最近5条评价
[操作区]   ♡收藏  |  预订  |  举报
[联系方式] "预订后可查看卖家联系方式"
```

#### 4.2.6 搜索功能

| 功能 | 说明 |
|------|------|
| 关键词搜索 | 匹配标题 + 描述（V1.0: MySQL LIKE; 后续升级: Elasticsearch IK分词） |
| 模糊搜索 | 支持部分关键词匹配 |
| 搜索结果排序 | 相关度（默认）/ 最新 / 价格↑ / 价格↓ |
| 搜索历史 | 本地存储最近10条 |
| 热门搜索 | 基于近7天搜索频率Top 10 |

**搜索无结果引导：**
- 提示"未找到相关商品，试试其他关键词"
- 展示"你可能感兴趣"（同分类热门商品，基于浏览量）
- 引导"发布求购"（跳转求购功能，V1.5实现）
- V1.0降级方案：引导用户切换分类浏览

**技术实现：**
- V1.0极简方案：MySQL LIKE模糊匹配 + 标题全文索引（接受初期搜索体验一般）
- 后续升级：Elasticsearch + IK Analyzer中文分词

#### 4.2.7 收藏

| 功能点 | 说明 |
|--------|------|
| 收藏操作 | 商品详情页点击♡收藏/取消收藏 |
| 收藏上限 | 100件（超限提示"收藏已满，请清理后再收藏"） |
| 收藏列表 | "我的收藏"中查看，分"在售"和"已售出"两个Tab |
| 过期提醒 | 收藏的商品被售出/下架后，标记灰显 + "已失效"标签 |
| 数据同步 | 收藏商品状态变更时实时更新 |

#### 4.2.8 预订

| 功能点 | 说明 |
|--------|------|
| 预订条件 | 已登录、信用分≥10、当前预订数<3 |
| 预订效果 | 商品状态→"已预订"，生成交易订单，**双方在订单详情页可见对方完整手机号和微信号** |
| 超时释放 | 预订后**24h**买家未确认交易且未取消 → 系统自动释放 → 商品恢复"在售" → 买家信用分-3 |
| 主动取消 | 买家可在"我的预订"中主动取消预订（**买家信用分-3**） |
| 卖家取消 | 卖家可在"我的发布"中取消预订（如联系不上买家），需填写原因，**卖家取消不扣买家信用分**（卖家有权拒绝交易），但取消原因记入订单 |
| 并发控制 | 使用Redis SETNX + DB乐观锁（version字段），确保同一商品只被一人预订 |

### 4.3 交易订单与确认

#### 4.3.1 订单状态流转

```
┌─────────┐
│  已预订   │
└────┬────┘
     │
     ├── 买家确认交易 ──→ ┌──────────┐
     │                    │  交易完成  │
     │                    └─────┬─────┘
     │                          │ 双方评价
     │                          ▼
     │                    ┌──────────┐
     │                    │  已评价   │
     │                    └──────────┘
     │
     ├── 买家取消 ──→ ┌──────────┐
     │                │  已取消   │
     │                └──────────┘
     │
     ├── 卖家取消 ──→ ┌──────────┐
     │                │  已取消   │
     │                └──────────┘
     │
     ├── "联系不上" ──→ ┌──────────┐  (卖家24h不响应)
     │  卖家不响应      │  已取消   │
     │                  └──────────┘
     │
     └── 24h超时 ──→ ┌──────────┐
                      │  已过期   │ ← 系统自动释放，商品回"在售"
                      └──────────┘
```

| 状态值 | 状态名 | 说明 |
|:------|--------|------|
| 1 | 已预订 | 买家已预订，24h内需确认或取消 |
| 2 | 交易完成 | 买家已确认线下交易完成 |
| 3 | 已取消 | 买家/卖家主动取消 / 联系不上自动取消 |
| 4 | 已过期 | 24h超时系统自动释放 |

#### 4.3.2 交易确认

| 场景 | 处理 |
|------|------|
| 买家主动确认 | 线下交易完成后，买家在"我的预订"中点击"确认交易完成"，商品状态→"已售出" |
| 卖家催确认 | 卖家可在"我的发布"中点击"提醒买家确认"（每天限1次），系统发送站内通知给买家 |
| 买家联系不上卖家 | 买家可在"我的预订"中点击"联系不上卖家"，卖家收到通知后24h未响应则订单自动取消 |
| 24h超时释放 | 预订后24h内买家无任何操作 → 系统自动释放预订，商品回"在售"，买家信用分-3 |

**说明**：V1.0 不再设72h自动确认已售出机制。如果线下已交易但买家忘记确认，卖家通过"催确认"按钮提醒。这避免了系统自动将未发生的交易误标为"已售出"。

### 4.4 评价系统

#### 4.4.1 评价规则

| 规则 | 说明 |
|------|------|
| 评价时机 | 买家确认交易完成后，双方均可评价 |
| 评价窗口 | 交易完成后7天内 |
| 超时处理 | 7天未评→自动默认好评 |
| 评价类型 | 好评(+1) / 中评(0) / 差评(-1) |
| 评价内容 | 可选文字（≤200字） |
| 评价可见性 | 双方评价内容在**双方都评价后**或**7天窗口结束后**才公开可见（防止报复性差评） |

#### 4.4.2 评价展示

- 用户主页：好评率 = 好评数 / 总评价数 × 100%
- 商品详情页：显示卖家最近5条有文字的评价
- 评价不可修改，但可申诉

#### 4.4.3 评价申诉

| 步骤 | 说明 |
|------|------|
| 1. 提交申诉 | 被评方在评价下点击"申诉"，填写理由+上传凭证 |
| 2. 管理员审核 | 3个工作日内处理 |
| 3. 申诉成立 | 隐藏该评价，不计入好评率，申诉方信用分不受影响 |
| 4. 申诉驳回 | 评价保留，计入用户**恶意申诉次数** |

**恶意申诉处罚：**
- 累计3次申诉被驳回 → 信用分-10 + 限制申诉权限30天
- 累计5次申诉被驳回 → 永久限制申诉功能 + 信用分-30

### 4.5 消息通知

#### 4.5.1 通知类型

| 通知类型 | 触发场景 | 通知方式 |
|---------|---------|---------|
| 预订通知 | 有人预订你的商品 / 你的预订成功 | 站内消息 + **微信订阅消息** |
| 释放通知 | 预订超时被释放 | 站内消息 |
| 催确认通知 | 卖家提醒你确认交易 | 站内消息 + **微信订阅消息** |
| 联系不上通知 | 买家反馈联系不上你 | 站内消息 + **微信订阅消息** |
| 评价通知 | 收到新评价 | 站内消息 |
| 系统通知 | 商品审核通过/驳回、商品即将过期 | 站内消息 |
| 举报通知 | 举报被处理 | 站内消息 |
| 处罚通知 | 违规处罚通知 | 站内消息 |
| 评价到期提醒 | 评价窗口还剩24h | 站内消息 + **微信订阅消息** |

#### 4.5.2 通知展示

- 个人中心"消息通知"入口 + 未读数字角标（小程序 tabBar 角标）
- 消息列表按时间排序，支持单条标记已读 + **一键全部已读**
- 保留最近30天消息，每天凌晨5点定时清理30天前的已读通知
- **微信订阅消息**：预订通知、催确认、交易完成等关键节点，通过微信订阅消息推送到用户微信（无需打开小程序即可收到提醒）。用户首次操作时授权订阅，每个场景可单独授权或关闭

### 4.6 举报机制

#### 4.6.1 举报类型

| 类型 | 说明 | 需上传凭证 |
|------|------|:--------:|
| 虚假商品 | 商品不存在/信息虚假 | 选填 |
| 描述不符 | 收到的商品与描述严重不符 | 必填 |
| 违禁品 | 香烟、管制刀具、药品、假证等 | 选填 |
| 广告骚扰 | 纯商业广告、引流外站 | 必填 |
| 联系方式违规 | 诈骗、骚扰 | 必填 |
| 其他违规 | 其他违反平台规则的行为 | 选填 |

#### 4.6.2 举报流程

```
用户提交举报 → 系统记录 → 管理员查证
                              ├── 属实 → 处理（下架/警告/封号）+ 通知举报人
                              └── 不实 → 驳回 + 记录原因
                    举报人可查看处理结果
```

- 管理员48h内处理
- 同一用户对同一商品的重复举报将被合并

#### 4.6.3 恶意举报防范

- 同一用户24h内最多举报3次
- 累计3次恶意举报 → 信用分-15 + 限制举报7天
- 累计5次恶意举报 → 永久限制举报功能 + 信用分-30

### 4.7 违规处罚阶梯

| 违规次数 | 处罚 |
|---------|------|
| 第1次 | 警告 + 违规内容下架 + 站内通知 |
| 第2次 | 限制发布7天 + 信用分-20 |
| 第3次 | 限制发布30天 + 信用分-30 |
| 第4次 | 永久封号 + 列入黑名单 |
| 严重违规 | 直接永久封号（违禁品、诈骗、人身攻击等） |

**申诉机制：**
- 所有处罚均附带申诉入口
- 被封号用户可通过邮件/客服渠道申诉
- 管理员3个工作日内复审

---

## 五、后台管理系统

### 5.1 角色权限矩阵

| 权限 | 超级管理员 | 运营管理员 | 客服管理员 | 内容审核员 |
|------|:--------:|:--------:|:--------:|:--------:|
| 管理员账号管理 | ✅ | ❌ | ❌ | ❌ |
| 查看完整用户手机号 | ✅ | ❌ | ❌ | ❌ |
| 用户禁用/解禁 | ✅ | ✅ | ❌ | ❌ |
| 重置用户昵称/头像 | ✅ | ✅ | ❌ | ❌ |
| 商品下架/恢复 | ✅ | ✅ | ❌ | ✅ |
| 敏感词管理 | ✅ | ✅ | ❌ | ❌ |
| 处理举报 | ✅ | ✅ | ✅ | ❌ |
| 处理评价申诉 | ✅ | ✅ | ✅ | ❌ |
| 内容审核（敏感词队列） | ✅ | ✅ | ❌ | ✅ |
| 查看数据统计 | ✅ | ✅ | ❌ | ❌ |
| 查看操作日志 | ✅ | ❌ | ❌ | ❌ |
| 导出用户数据 | ✅ | ❌ | ❌ | ❌ |

### 5.2 用户管理

- 用户列表：按学号/手机号/昵称搜索，按注册时间/状态/信用分筛选
- 用户详情：基本信息 + 发布历史 + 交易记录 + 评价记录 + 举报记录
- 操作：禁用/启用、重置昵称/头像、查看脱敏手机号（超管可看完整号）
- 导出：支持CSV导出（手机号自动脱敏，超管可选择脱敏或完整；openid 不导出，仅超管可在详情页查看）

### 5.3 商品管理

- 商品列表：搜索、分类/状态/时间筛选、批量操作
- 违规处理：强制下架 + 填写下架原因 + 系统自动通知卖家
- 敏感词审核队列：按提交时间排序，支持批量通过/驳回
- 商品编辑：管理员可修改标题/描述（标记"管理员已编辑"）

### 5.4 举报管理

- 待处理列表：按提交时间排序，支持按举报类型筛选
- 处理操作：通过（选择处罚措施）/ 驳回（填写驳回原因）
- 处理记录：含受理人、时间、处理结果
- 统计看板：举报类型分布、处理率、平均处理时间

### 5.5 评价申诉管理

- 待处理申诉列表：按提交时间排序
- 处理操作：通过（隐藏评价）/ 驳回（保留评价并计入恶意申诉）
- 申诉统计：申诉类型分布、通过率、用户恶意申诉次数排名

### 5.6 敏感词管理

- 增删改查：词条、分类、状态（启用/禁用）
- 批量导入：CSV/Excel批量导入
- 分类：违禁品、广告引流、色情、政治敏感、其他
- 匹配方式：精确匹配 + 模糊匹配（包含即命中）

### 5.7 数据统计

| 指标分类 | 具体指标 | 时间维度 |
|---------|---------|---------|
| 用户 | 注册数、DAU、WAU、留存率、信用分分布 | 日/周/月 |
| 商品 | 发布数、在售数、成交数、成交率、分类占比 | 日/周/月 |
| 交易 | 预订数、确认数、释放数、成交率、平均成交周期 | 日/周/月 |
| 内容 | PV、UV、搜索量、热门搜索词、收藏量 | 日/周/月 |
| 运营 | 举报量、处理率、违规率、平均处理时长 | 日/周/月 |

### 5.8 操作日志

- 记录字段：操作人、操作时间、操作类型、操作对象、操作IP、操作详情
- 日志保留：180天（满足纠纷追溯需求，≥90天基础上加码）
- 不可删除，不可篡改（日志表只做INSERT，不做UPDATE/DELETE）

---

## 六、非功能性需求

### 6.1 性能指标

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| 页面首次加载（列表页） | ≤ 2s（3G网络），≤ 1s（WiFi） | Lighthouse |
| 搜索响应时间 | ≤ 500ms（P95） | 接口耗时监控 |
| 图片加载（缩略图） | ≤ 300ms | 图片CDN响应时间 |
| 接口响应时间（P99） | ≤ 1s | APM监控 |
| 并发支持 | 500用户同时在线无异常 | JMeter压测 |
| 系统可用性 | ≥ 99.5%（月） | 监控告警 |

### 6.2 兼容性

| 平台 | 要求 |
|------|------|
| 微信小程序 | 微信客户端 ≥ 8.0，基础库 ≥ 2.20.0 |
| Web 端（H5） | Chrome 90+ / Edge 90+ / Safari iOS 14+ / 微信内置浏览器 |
| 后台管理（Web） | Chrome 90+ / Edge 90+ |

### 6.3 安全性需求

| 需求 | 实现方式 |
|------|---------|
| 传输加密 | 全站HTTPS（强制TLS 1.2+，小程序强制要求） |
| 身份认证 | 小程序端：微信 openid + JWT；Web端：手机号+密码(BCrypt) + JWT |
| 密码安全 | BCrypt 加密存储，密码复杂度要求（大小写字母+数字，8-20位） |
| 会话管理 | Redis 存储 token，30天过期，支持服务端主动踢出 |
| SQL注入防护 | MyBatis 参数化查询（#{}不使用${}） |
| XSS防护 | 用户输入 HTML 实体转义；CSP 头（Web端） |
| CSRF防护 | Web端：SameSite Cookie + CSRF Token；小程序端：Token Header 传递，天然防 CSRF |
| 文件上传安全 | 校验文件魔术数字（Magic Number），拒绝非图片文件；限制文件大小 |
| 接口限流 | 基于用户ID + IP的令牌桶限流 |
| 敏感数据脱敏 | 日志中手机号/密码自动过滤；管理后台手机号分级展示 |
| 联系方式加密 | 商品表/用户表中的手机号采用**HMAC盲索引 + AES-GCM随机IV加密**分离存储：`phone_hash`字段(HMAC-SHA256)用于WHERE等值查询，`phone`字段(AES-GCM)用于解密展示；微信号/QQ号使用AES-GCM加密存储 |
| 微信数据安全 | 遵循《微信小程序平台运营规范》，openid 加密存储，管理后台不展示 |

### 6.4 可用性

- P0故障（核心功能不可用）：15分钟内响应，1小时内恢复
- P1故障（非核心功能不可用）：30分钟内响应，4小时内恢复
- 每日凌晨2点自动备份数据库，备份保留7天

---

## 七、技术架构

### 7.1 技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| **双端前端** | **uni-app (Vue 3)** + uView UI | uni-app 3.x | 一套代码编译为**微信小程序**和 **H5 Web**；Vue 3 语法，AI 生成效率高 |
| 后台管理前端 | Element Plus | 最新版 | 成熟的中后台 UI 库 |
| 后端 | Java + Spring Boot + MyBatis-Plus | Spring Boot 2.7+ | 生态成熟、MyBatis-Plus 减少样板代码 |
| 数据库 | MySQL | 8.0 | 成熟稳定，满足预估数据量 |
| 缓存 | Redis | 7.0 | 会话缓存、热点数据、分布式锁 |
| 搜索 | MySQL LIKE 极简方案 → 后续升级 ES | — | V1.0 快速上线 |
| 图片存储 | 本地存储（V1.0） → 后续升级 OSS+CDN | — | 双端均上传至后端统一处理 |
| 微信能力 | 微信登录、订阅消息、内容安全API（仅小程序端） | — | 条件编译处理平台差异 |
| 短信服务 | 阿里云/腾讯云 SMS | — | 注册、Web端登录验证 |
| 部署 | Nginx + Spring Boot Jar（后端+Web静态资源） | Nginx 1.24 | 单服务器，Nginx 代理 API + 托管 H5 静态文件 |
| 监控 | 日志文件（V1.0），Prometheus + Grafana（V1.5） | — | 渐进式接入 |

**为什么选 uni-app 双端编译：**
1. 一套 Vue 3 代码，条件编译（`#ifdef MP-WEIXIN` / `#ifdef H5`）处理平台差异
2. uView UI 同时支持小程序和 H5 渲染
3. AI 对 uni-app (Vue 3) 的代码生成质量远高于原生 WXML/WXSS
4. 小程序端享受微信生态（分享、订阅消息），Web 端覆盖非微信场景（PC 浏览、QQ/微博分享打开）
5. 个人开发者只需维护一套前端代码，效率翻倍

### 7.2 后端分层架构

```
┌─────────────────┐
│   VO / DTO      │  ← 对外暴露的数据结构（与Entity解耦）
├─────────────────┤
│   Controller    │  ← 参数校验(@Validated)、权限校验、调用Service
├─────────────────┤
│   Service       │  ← 业务逻辑、事务管理、缓存操作
├─────────────────┤
│   Mapper        │  ← MyBatis-Plus数据访问
├─────────────────┤
│   MySQL / Redis │  ← 数据存储
└─────────────────┘
```

**统一响应格式：**
```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1717718400000,
  "requestId": "uuid"
}
```

**错误码规范：**

| Code | 含义 | 说明 |
|------|------|------|
| 200 | 成功 | — |
| 400 | 参数错误 | 前端参数校验失败 |
| 401 | 未登录 | Token过期或无效 |
| 403 | 无权限 | 权限不足 |
| 404 | 资源不存在 | 商品/用户等不存在 |
| 409 | 冲突 | 重复预订/并发冲突 |
| 429 | 请求过于频繁 | 触发限流 |
| 500 | 服务器内部错误 | 未知异常 |

**全局异常处理：**
- `@RestControllerAdvice` 统一拦截异常
- 业务异常 `BusinessException(code, message)` 
- 参数校验异常 `MethodArgumentNotValidException` → 400 + 具体字段错误
- 未捕获异常 → 500 + 隐藏内部错误详情（仅日志记录）

### 7.3 数据库设计

#### 7.3.1 数据表清单

| 表名 | 说明 | 预估量 |
|------|------|--------|
| `sys_user` | 用户表 | 1万 |
| `sys_admin` | 管理员表 | 10 |
| `goods_category` | 商品分类表 | 50 |
| `goods_info` | 商品主表 | 5万 |
| `goods_image` | 商品图片表 | 20万 |
| `trade_order` | 交易订单表 | 1万 |
| `user_favorite` | 用户收藏表 | 10万 |
| `user_evaluation` | 用户评价表 | 5万 |
| `report_record` | 举报记录表 | 1万 |
| `sensitive_word` | 敏感词表 | 1000 |
| `user_notification` | 用户通知表 | 20万 |
| `user_browse_history` | 用户浏览记录表 | 50万 |
| `admin_log` | 管理员操作日志表 | 10万 |
| `sms_log` | 短信发送日志表 | 5万 |
| `credit_log` | 信用分变更日志表 | 10万 |
| `daily_statistics` | 每日统计聚合表（V1.0可选） | 365行/年 |

#### 7.3.2 核心表结构

**sys_user（用户表）**
```sql
CREATE TABLE sys_user (
    user_id            BIGINT        PRIMARY KEY AUTO_INCREMENT,
    student_id         VARCHAR(20)   NOT NULL UNIQUE  COMMENT '学号',
    openid             VARCHAR(50)                    COMMENT '微信openid(小程序端绑定，Web端为NULL)',
    unionid            VARCHAR(50)                    COMMENT '微信unionid(预留)',
    phone              VARCHAR(200)  NOT NULL         COMMENT '手机号(AES-GCM随机IV加密存储，仅用于解密展示)',
    phone_hash         VARCHAR(64)   NOT NULL         COMMENT '手机号HMAC盲索引(用于WHERE等值查询)',
    password           VARCHAR(100)                   COMMENT '密码(BCrypt，Web端必须，小程序端可选)',
    nickname           VARCHAR(24)   NOT NULL         COMMENT '昵称',
    avatar             VARCHAR(200)                   COMMENT '头像URL',
    credit_score       INT           DEFAULT 60       COMMENT '信用分(0-100)',
    status             TINYINT       DEFAULT 1        COMMENT '0禁用 1正常 2注销',
    cancel_status      TINYINT       DEFAULT 0        COMMENT '注销状态 0正常 1注销申请中 2已注销',
    cancel_at          DATETIME                       COMMENT '注销申请提交时间',
    malicious_appeal_count INT       DEFAULT 0        COMMENT '恶意申诉累计次数',
    last_login_ip      VARCHAR(50)                    COMMENT '最后登录IP',
    last_login_at      DATETIME                       COMMENT '最后登录时间',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at         DATETIME      ON UPDATE CURRENT_TIMESTAMP,
    is_deleted         TINYINT       DEFAULT 0        COMMENT '逻辑删除',
    INDEX idx_student_id (student_id),
    INDEX idx_openid (openid),     -- 非唯一索引：Web端用户openid=NULL
    UNIQUE KEY uk_phone_hash (phone_hash),  -- 唯一约束改到HMAC盲索引
    INDEX idx_status (status),
    INDEX idx_credit_score (credit_score)
) COMMENT '用户表(双端：小程序openid绑定 + Web手机号密码；手机号HMAC盲索引+AES-GCM加密分离)';
```

**sys_admin（管理员表）**
```sql
CREATE TABLE sys_admin (
    admin_id           BIGINT        PRIMARY KEY AUTO_INCREMENT,
    username           VARCHAR(30)   NOT NULL UNIQUE  COMMENT '登录用户名',
    password           VARCHAR(100)  NOT NULL         COMMENT '密码(BCrypt)',
    nickname           VARCHAR(24)   NOT NULL         COMMENT '显示名称',
    role               TINYINT       NOT NULL         COMMENT '1超管 2运营 3客服 4审核',
    status             TINYINT       DEFAULT 1        COMMENT '0禁用 1正常',
    last_login_ip      VARCHAR(50)                    COMMENT '最后登录IP',
    last_login_at      DATETIME                       COMMENT '最后登录时间',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at         DATETIME      ON UPDATE CURRENT_TIMESTAMP
) COMMENT '管理员表';
```

**goods_category（商品分类表）**
```sql
CREATE TABLE goods_category (
    category_id        INT           PRIMARY KEY AUTO_INCREMENT,
    parent_id          INT           DEFAULT 0        COMMENT '父分类ID(0=一级分类)',
    name               VARCHAR(30)   NOT NULL         COMMENT '分类名称',
    sort_order         INT           DEFAULT 0        COMMENT '排序权重',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP
) COMMENT '商品分类表';
```

**goods_info（商品表）**
```sql
CREATE TABLE goods_info (
    goods_id         BIGINT        PRIMARY KEY AUTO_INCREMENT,
    user_id          BIGINT        NOT NULL          COMMENT '发布者ID',
    category_id      INT           NOT NULL          COMMENT '二级分类ID',
    title            VARCHAR(60)   NOT NULL          COMMENT '标题(4-30字)',
    description      TEXT                             COMMENT '描述(10-500字)',
    price            INT           NOT NULL          COMMENT '售价(元)',
    original_price   INT                             COMMENT '原价(选填)',
    goods_condition  TINYINT       NOT NULL          COMMENT '1全新 2 99新 3 9成新 4 8成新 5战损 6故障',
    trade_location   VARCHAR(50)                     COMMENT '交易地点',
    contact_phone    VARCHAR(200)                    COMMENT '联系手机号(AES-GCM随机IV加密)',
    contact_wechat   VARCHAR(200)                    COMMENT '微信号(AES-GCM随机IV加密)',
    contact_qq       VARCHAR(200)                    COMMENT 'QQ号(AES-GCM随机IV加密)',
    status           TINYINT       DEFAULT 1         COMMENT '0下架 1在售 2已预订 3已售出',
    view_count       INT           DEFAULT 0         COMMENT '浏览量',
    version          INT           DEFAULT 0         COMMENT '乐观锁版本号(@Version)',
    last_relisted_at DATETIME                        COMMENT '最近上架时间(用于30天自动下架计算，发布/重新上架时更新)',
    audit_status     TINYINT       DEFAULT 1         COMMENT '0审核中 1通过 2驳回',
    created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at       DATETIME      ON UPDATE CURRENT_TIMESTAMP,
    is_deleted       TINYINT       DEFAULT 0         COMMENT '逻辑删除',
    INDEX idx_user_id (user_id),
    INDEX idx_category_id (category_id),
    INDEX idx_status (status),
    INDEX idx_price (price),
    INDEX idx_created_at (created_at),
    INDEX idx_audit_status (audit_status)
) COMMENT '商品表';
```

> **联系方式说明**：发布商品时，系统读取用户注册手机号作为默认值，用户可修改或追加微信号。存储时使用AES-GCM随机IV加密，仅当订单处于"已预订"状态时，在订单详情接口中解密展示给交易双方。

**goods_image（商品图片表）**
```sql
CREATE TABLE goods_image (
    image_id           BIGINT        PRIMARY KEY AUTO_INCREMENT,
    goods_id           BIGINT        NOT NULL          COMMENT '商品ID',
    image_url          VARCHAR(200)  NOT NULL          COMMENT '图片URL',
    thumb_url          VARCHAR(200)                    COMMENT '缩略图URL',
    sort_order         INT           DEFAULT 0         COMMENT '排序(首图=0)',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_goods_id (goods_id)
) COMMENT '商品图片表';
```

**trade_order（交易订单表）**
```sql
CREATE TABLE trade_order (
    order_id         BIGINT        PRIMARY KEY AUTO_INCREMENT,
    goods_id         BIGINT        NOT NULL          COMMENT '商品ID',
    buyer_id         BIGINT        NOT NULL          COMMENT '买家ID',
    seller_id        BIGINT        NOT NULL          COMMENT '卖家ID',
    buyer_phone      VARCHAR(100)                    COMMENT '买家手机号(AES-GCM加密，预订时快照)',
    seller_phone     VARCHAR(100)                    COMMENT '卖家手机号(AES-GCM加密，预订时快照)',
    seller_wechat    VARCHAR(100)                    COMMENT '卖家微信号(AES-GCM加密，预订时快照)',
    seller_qq        VARCHAR(100)                    COMMENT '卖家QQ号(AES-GCM加密，预订时快照)',
    status           TINYINT       DEFAULT 1         COMMENT '1已预订 2交易完成 3已取消 4已过期',
    booked_at        DATETIME                         COMMENT '预订时间',
    confirmed_at     DATETIME                         COMMENT '交易确认时间',
    cancelled_at     DATETIME                         COMMENT '取消时间',
    cancel_reason    VARCHAR(200)                     COMMENT '取消原因',
    cancel_by        TINYINT                          COMMENT '取消方 1买家 2卖家 3系统',
    contact_fail_at  DATETIME                         COMMENT '买家提交联系不上的时间(用于24h倒计时)',
    released_at      DATETIME                         COMMENT '自动释放时间',
    created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_goods_id (goods_id),
    INDEX idx_buyer_id (buyer_id),
    INDEX idx_seller_id (seller_id),
    INDEX idx_status (status),
    INDEX idx_booked_at (booked_at)
) COMMENT '交易订单表';
```

> **联系方式的快照机制**：预订时快照买卖双方的手机号、微信号和QQ号到订单表。这样即使交易完成后用户修改了联系方式，订单中的历史联系信息也不会丢失，便于后续追溯。

**user_favorite（用户收藏表）**
```sql
CREATE TABLE user_favorite (
    favorite_id        BIGINT        PRIMARY KEY AUTO_INCREMENT,
    user_id            BIGINT        NOT NULL          COMMENT '用户ID',
    goods_id           BIGINT        NOT NULL          COMMENT '商品ID',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_goods (user_id, goods_id),
    INDEX idx_user_id (user_id),
    INDEX idx_goods_id (goods_id)
) COMMENT '用户收藏表';
```

**user_evaluation（用户评价表）**
```sql
CREATE TABLE user_evaluation (
    eval_id          BIGINT        PRIMARY KEY AUTO_INCREMENT,
    order_id         BIGINT        NOT NULL          COMMENT '关联订单ID',
    evaluator_id     BIGINT        NOT NULL          COMMENT '评价人ID',
    target_id        BIGINT        NOT NULL          COMMENT '被评价人ID',
    rating           TINYINT       NOT NULL          COMMENT '1好评 2中评 3差评',
    content          VARCHAR(200)                    COMMENT '评价内容',
    is_auto          TINYINT       DEFAULT 0         COMMENT '0手动 1超时自动默认好评',
    appeal_status    TINYINT       DEFAULT 0         COMMENT '0正常 1申诉中 2申诉通过(隐藏) 3申诉驳回',
    created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_target_id (target_id),
    INDEX idx_order_id (order_id),
    UNIQUE KEY uk_order_evaluator (order_id, evaluator_id)  COMMENT '同一订单每人只能评价一次'
) COMMENT '评价表';
```

**report_record（举报记录表）**
```sql
CREATE TABLE report_record (
    report_id          BIGINT        PRIMARY KEY AUTO_INCREMENT,
    reporter_id        BIGINT        NOT NULL          COMMENT '举报人ID',
    goods_id           BIGINT        NOT NULL          COMMENT '被举报商品ID',
    type               TINYINT       NOT NULL          COMMENT '1虚假商品 2描述不符 3违禁品 4广告骚扰 5联系方式违规 6其他',
    description        VARCHAR(500)                    COMMENT '举报说明',
    evidence_urls      VARCHAR(1000)                   COMMENT '凭证图片URL(JSON数组)',
    status             TINYINT       DEFAULT 0         COMMENT '0待处理 1通过 2驳回',
    handler_id         BIGINT                          COMMENT '处理人ID',
    handle_result      VARCHAR(500)                    COMMENT '处理结果说明',
    handled_at         DATETIME                         COMMENT '处理时间',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_reporter_id (reporter_id),
    INDEX idx_goods_id (goods_id),
    INDEX idx_status (status)
) COMMENT '举报记录表';
```

**sensitive_word（敏感词表）**
```sql
CREATE TABLE sensitive_word (
    word_id            INT           PRIMARY KEY AUTO_INCREMENT,
    word               VARCHAR(50)   NOT NULL          COMMENT '敏感词',
    category           TINYINT       NOT NULL          COMMENT '1违禁品 2广告 3色情 4政治 5违规药品 6赌博',
    status             TINYINT       DEFAULT 1         COMMENT '0禁用 1启用',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_word (word)
) COMMENT '敏感词表';
```

**admin_log（管理员操作日志表）**
```sql
CREATE TABLE admin_log (
    log_id             BIGINT        PRIMARY KEY AUTO_INCREMENT,
    admin_id           BIGINT        NOT NULL          COMMENT '操作人ID',
    action_type        VARCHAR(50)   NOT NULL          COMMENT '操作类型(如USER_BAN/GOODS_OFFLINE/REPORT_HANDLE等)',
    target_type        VARCHAR(50)                     COMMENT '操作对象类型(如USER/GOODS/REPORT等)',
    target_id          BIGINT                          COMMENT '操作对象ID',
    detail             VARCHAR(500)                    COMMENT '操作详情(JSON)',
    ip                 VARCHAR(50)                     COMMENT '操作IP',
    created_at         DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_admin_id_time (admin_id, created_at),
    INDEX idx_action_type (action_type),
    INDEX idx_created_at (created_at)
) COMMENT '管理员操作日志表(只INSERT不UPDATE/DELETE)';
```

**user_notification（用户通知表）**
```sql
CREATE TABLE user_notification (
    notif_id         BIGINT        PRIMARY KEY AUTO_INCREMENT,
    user_id          BIGINT        NOT NULL          COMMENT '接收用户ID',
    type             TINYINT       NOT NULL          COMMENT '1预订 2释放 3催确认 4联系不上 5评价 6系统 7举报 8处罚 9评价到期',
    title            VARCHAR(100)  NOT NULL          COMMENT '通知标题',
    content          VARCHAR(500)                    COMMENT '通知内容',
    related_id       BIGINT                          COMMENT '关联业务ID(商品/订单/评价等)',
    is_read          TINYINT       DEFAULT 0         COMMENT '0未读 1已读',
    created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id_read (user_id, is_read),
    INDEX idx_created_at (created_at)
) COMMENT '用户通知表';
```

**user_browse_history（用户浏览记录表）**
```sql
CREATE TABLE user_browse_history (
    history_id       BIGINT        PRIMARY KEY AUTO_INCREMENT,
    user_id          BIGINT        NOT NULL          COMMENT '用户ID',
    goods_id         BIGINT        NOT NULL          COMMENT '商品ID',
    browse_count     INT           DEFAULT 1         COMMENT '浏览次数',
    last_browse_at   DATETIME      DEFAULT CURRENT_TIMESTAMP
                                                      COMMENT '最近浏览时间',
    UNIQUE KEY uk_user_goods (user_id, goods_id),
    INDEX idx_user_id_last (user_id, last_browse_at)
) COMMENT '用户浏览记录表';
```

**sms_log（短信发送日志表）**
```sql
CREATE TABLE sms_log (
    sms_id           BIGINT        PRIMARY KEY AUTO_INCREMENT,
    phone            VARCHAR(20)   NOT NULL          COMMENT '接收手机号',
    ip               VARCHAR(50)                     COMMENT '请求IP',
    type             TINYINT       NOT NULL          COMMENT '1注册 2登录 3修改密码 4其他',
    code             VARCHAR(10)                     COMMENT '验证码(可配置不记录)',
    result           TINYINT       DEFAULT 0         COMMENT '0成功 1失败',
    fail_reason      VARCHAR(100)                     COMMENT '失败原因',
    created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_time (phone, created_at),
    INDEX idx_ip_time (ip, created_at)
) COMMENT '短信发送日志表(双端注册时使用)';
```

**credit_log（信用分变更日志表）**
```sql
CREATE TABLE credit_log (
    log_id          BIGINT        PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT        NOT NULL          COMMENT '用户ID',
    delta           INT           NOT NULL          COMMENT '变更分值(正加负减)',
    before_score    INT           NOT NULL          COMMENT '变更前分数',
    after_score     INT           NOT NULL          COMMENT '变更后分数',
    reason          VARCHAR(100)  NOT NULL          COMMENT '变更原因',
    related_id      BIGINT                          COMMENT '关联业务ID(订单ID/举报ID等)',
    related_type    VARCHAR(20)                     COMMENT '关联类型(ORDER/REPORT/EVALUATION/AUTO)',
    operator        TINYINT       DEFAULT 0         COMMENT '0系统 1管理员',
    operator_id     BIGINT                          COMMENT '管理员ID(operator=1时)',
    created_at      DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id_time (user_id, created_at),
    INDEX idx_related (related_type, related_id)
) COMMENT '信用分变更日志表(只INSERT不UPDATE/DELETE)';
```

### 7.4 Redis 使用场景

| 场景 | Key格式 | 过期时间 | 说明 |
|------|---------|---------|------|
| 用户Session | `session:{token}` | 30天 | 存储userId、roles等 |
| 微信登录态 | `wx:session:{openid}` | 1天 | 存储微信session_key（每次wx-login刷新，用于解密手机号等敏感数据） |
| 热门商品缓存 | `hot:goods:list` | 10分钟 | 首页推荐 |
| 商品浏览量 | `goods:views:{goods_id}` | 1小时 | 定时同步到DB |
| 搜索热词 | `search:hotwords` | 1小时 | ZSET，按搜索次数排序 |
| 预订锁 | `booking:lock:{goods_id}` | 30秒 | SETNX防并发预订 |
| IP限流 | `ratelimit:ip:{ip}:{api}` | 1分钟 | 令牌桶 |
| 用户限流 | `ratelimit:uid:{uid}:{api}` | 1分钟 | 令牌桶 |
| 预订超时队列 | `booking:timeout:queue` | — | Sorted Set，score=预订时间+24h，定时任务扫描 |
| 释放任务分布式锁 | `booking:release-job:lock` | 120秒 | 多实例部署时确保只有一个实例执行释放扫描 |
| 联系不上超时队列 | `booking:contact-fail:queue` | — | Sorted Set，score=contactFailAt+24h，定时任务扫描 |
| 登录失败计数 | `login:fail:{phone}` | 15分钟 | 连续密码错误计数，≥5次锁定账号 |

### 7.5 缓存策略

| 风险 | 应对 |
|------|------|
| 缓存穿透 | 对不存在的数据也缓存空值（TTL 1分钟）；布隆过滤器（V1.5） |
| 缓存击穿 | 热点数据加互斥锁，同一时刻只有一个线程重建缓存 |
| 缓存雪崩 | 缓存过期时间加随机偏移（±30%）；多级缓存 |

### 7.6 并发控制与分布式调度

**预订并发控制（关键）：**
```
1. 使用Redis SETNX获取预订锁 booking:lock:{goods_id}（TTL 30s）
2. 获取锁成功 → 检查DB中商品状态是否为"在售"(status=1)
3. 状态为"在售" → 更新状态为"已预订"（UPDATE ... WHERE status=1 AND version=?）
4. 更新成功 → 创建订单 → 将订单加入超时队列（24h后检查）
5. finally → 释放锁
```
- 数据库层：乐观锁（version字段）兜底
- 缓存层：Redis分布式锁防并发

**预订超时释放（多实例安全）：**
```
定时任务（每分钟触发）:
1. 尝试获取释放任务锁 booking:release-job:lock（SETNX, TTL 120s）
2. 获取锁失败 → 说明其他实例正在执行，跳过本次
3. 获取锁成功 → 从超时队列 ZRANGEBYSCORE 取出已过期的订单
4. 逐笔处理：更新订单状态为"已过期" + 释放商品（UPDATE ... WHERE version=?）
5. 处理完成 → 从队列中 ZREM 已处理的订单
6. 释放任务锁
```
- 使用 Redis 分布式锁确保多实例部署下只有一个节点执行释放任务
- 乐观锁兜底，即使锁意外失效也不会超卖
- 后台管理提供"手动释放"按钮作为应急预案

### 7.7 部署方案

```
┌──────────────┐     ┌──────────────┐
│ 微信小程序端  │     │   Web 浏览器  │
│ (微信托管)    │     │ (H5 SPA)     │
└──────┬───────┘     └──────┬───────┘
       │ HTTPS               │ HTTPS
       └──────────┬──────────┘
                  ▼
┌─────────────────────────────────┐
│             Nginx                │  ← 反向代理 + SSL + H5静态文件
│  /api/* → Spring Boot           │
│  /*     → H5静态文件(/dist/h5/)  │
│  /images/* → 本地图片目录        │
└────────────────┬────────────────┘
                 │
        ┌────────┼────────┐
        ▼        ▼        ▼
  ┌────────┐┌────────┐┌──────────┐
  │Spring  ││ Redis  ││  本地磁盘 │  ← 图片存储
  │Boot Jar││        ││ /data/    │
  └───┬────┘└────────┘└──────────┘
      │
      ▼
  ┌────────┐
  │ MySQL  │
  └────────┘
```

**双端部署说明：**
- 小程序端：uni-app 编译为微信小程序 → 提交微信审核 → 微信托管
- Web 端：uni-app 编译为 H5 → 静态文件部署到 Nginx
- 后端：Spring Boot 单 jar 包，Nginx 代理 API 请求
- 图片：统一上传至后端本地存储，Nginx 代理静态访问
- 小程序强制要求 HTTPS 域名，Web 端同样走 HTTPS

**备份策略：**
- 数据库：每日凌晨2点 `mysqldump` + 保留7天滚动
- 图片文件：每日增量同步至云存储（rsync 到云服务器或百度网盘）
- Redis：RDB快照，每6小时一次

**容灾方案：**

| 故障 | 恢复方案 | RTO | RPO |
|------|---------|-----|-----|
| 应用服务器宕机 | 重启 jar 包（systemd 自动拉起） | 1分钟 | 0 |
| MySQL宕机 | 从备份恢复（V1.0单点，V1.5做主从） | 2小时 | 24小时 |
| Redis宕机 | 重启 + RDB恢复 | 10分钟 | 6小时 |
| 图片存储故障 | 从云备份恢复 | 1小时 | 24小时 |

---

## 八、接口规范

### 8.1 接口设计原则

- RESTful风格，路径含版本号：`/api/v1/...`
- 请求体：JSON格式（Content-Type: application/json）
- 文件上传：双端统一通过 `multipart/form-data` 上传至后端 `/api/v1/upload`
- 认证方式：Header携带 `Authorization: Bearer {token}`（token 由微信登录或手机号登录接口签发，双端统一格式）
- **可选认证**：标注"可选"的接口（如商品列表/详情/搜索），无token时正常返回数据但不包含用户相关字段（如isFavorited）；有token时返回完整字段
- 分页参数：`?page=1&size=20`
- 所有时间字段使用ISO 8601格式

### 8.2 错误响应统一格式

```json
{
  "code": 400,
  "message": "参数错误",
  "data": null,
  "errors": [
    { "field": "studentId", "message": "学号格式不正确" }
  ],
  "timestamp": "2026-06-07T12:00:00+08:00",
  "requestId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
}
```

### 8.3 用户端接口

#### 用户模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| POST | `/api/v1/auth/wx-login` | **小程序微信登录**（code → token） | ❌ |
| POST | `/api/v1/auth/login` | **Web端登录**（手机号+密码） | ❌ |
| POST | `/api/v1/auth/register` | 注册（手机号+学号+密码，小程序端传openid绑定） | ❌ |
| GET | `/api/v1/auth/captcha` | 获取图形验证码（短信发送前） | ❌ |
| POST | `/api/v1/auth/sms-code` | 发送短信验证码 | ❌ |
| POST | `/api/v1/auth/wx-bind` | **Web端绑定微信openid**（扫码后绑定） | ✅ |
| POST | `/api/v1/auth/logout` | 退出登录 | ✅ |
| GET | `/api/v1/user/profile` | 获取个人信息 | ✅ |
| PUT | `/api/v1/user/profile` | 修改个人信息（头像/昵称/密码） | ✅ |
| POST | `/api/v1/user/cancel` | 申请注销账号 | ✅ |
| PUT | `/api/v1/user/cancel/revoke` | 撤销注销 | ✅ |
| GET | `/api/v1/user/{id}/profile` | 查看他人公开信息 | ✅ |

#### 商品模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| GET | `/api/v1/goods` | 商品列表（分页+筛选） | 可选 |
| GET | `/api/v1/goods/{id}` | 商品详情 | 可选 |
| POST | `/api/v1/goods` | 发布商品 | ✅ |
| PUT | `/api/v1/goods/{id}` | 编辑商品 | ✅ |
| POST | `/api/v1/goods/{id}/offline` | 下架商品 | ✅ |
| POST | `/api/v1/goods/{id}/relist` | 重新上架 | ✅ |
| GET | `/api/v1/goods/search` | 搜索商品 | 可选 |
| GET | `/api/v1/goods/my` | 我的发布 | ✅ |

#### 交易模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| POST | `/api/v1/trade/book/{goodsId}` | 预订商品 | ✅ |
| POST | `/api/v1/trade/confirm/{orderId}` | 确认交易完成 | ✅ |
| POST | `/api/v1/trade/cancel/{orderId}` | 取消预订 | ✅ |
| POST | `/api/v1/trade/{orderId}/remind` | 卖家提醒买家确认 | ✅ |
| POST | `/api/v1/trade/{orderId}/contact-fail` | 买家反馈联系不上卖家 | ✅ |
| POST | `/api/v1/trade/{orderId}/seller-respond` | 卖家响应"已联系买家" | ✅ |
| GET | `/api/v1/trade/my-orders` | 我的交易订单 | ✅ |
| GET | `/api/v1/trade/order/{id}` | 订单详情（含双方联系方式） | ✅ |

#### 评价模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| POST | `/api/v1/evaluation` | 发表评价 | ✅ |
| GET | `/api/v1/evaluation/user/{id}` | 查看用户评价列表 | ❌ |
| POST | `/api/v1/evaluation/{id}/appeal` | 评价申诉 | ✅ |

#### 收藏/通知/浏览记录模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| POST | `/api/v1/favorite/{goodsId}` | 收藏/取消收藏 | ✅ |
| GET | `/api/v1/favorite/my` | 我的收藏列表 | ✅ |
| GET | `/api/v1/notification` | 通知列表 | ✅ |
| PUT | `/api/v1/notification/{id}/read` | 单条标记已读 | ✅ |
| PUT | `/api/v1/notification/read-all` | 全部标记已读 | ✅ |
| GET | `/api/v1/notification/unread-count` | 未读数 | ✅ |
| GET | `/api/v1/browse-history` | 浏览记录列表 | ✅ |

#### 举报模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:--:|
| POST | `/api/v1/report` | 提交举报 | ✅ |
| GET | `/api/v1/report/my` | 我的举报列表 | ✅ |

### 8.4 后台管理接口

#### 管理员认证

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| POST | `/api/v1/admin/auth/login` | 管理员登录 | 公开 |
| POST | `/api/v1/admin/auth/logout` | 管理员退出 | 管理员 |

#### 用户管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/users` | 用户列表（搜索+筛选+分页） | 运营管理员 |
| GET | `/api/v1/admin/users/{id}` | 用户详情 | 运营管理员 |
| PUT | `/api/v1/admin/users/{id}/status` | 禁用/启用用户 | 运营管理员 |
| PUT | `/api/v1/admin/users/{id}/reset-profile` | 重置昵称/头像 | 运营管理员 |
| GET | `/api/v1/admin/users/export` | 导出用户数据(CSV) | 超管 |

#### 商品管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/goods` | 商品列表 | 运营/审核 |
| GET | `/api/v1/admin/goods/{id}` | 商品详情 | 运营/审核 |
| PUT | `/api/v1/admin/goods/{id}/offline` | 强制下架商品 | 运营/审核 |
| PUT | `/api/v1/admin/goods/{id}` | 编辑商品信息 | 运营管理员 |
| POST | `/api/v1/admin/goods/release/{goodsId}` | 手动释放预订商品 | 运营管理员 |

#### 审核管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/audit/queue` | 待审核队列 | 运营/审核 |
| POST | `/api/v1/admin/audit/{id}/approve` | 审核通过 | 运营/审核 |
| POST | `/api/v1/admin/audit/{id}/reject` | 审核驳回 | 运营/审核 |

#### 举报/申诉管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/reports` | 举报列表 | 运营/客服 |
| GET | `/api/v1/admin/reports/{id}` | 举报详情 | 运营/客服 |
| POST | `/api/v1/admin/reports/{id}/handle` | 处理举报 | 运营/客服 |
| GET | `/api/v1/admin/appeals` | 评价申诉列表 | 运营/客服 |
| POST | `/api/v1/admin/appeals/{id}/handle` | 处理申诉 | 运营/客服 |

#### 敏感词管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/sensitive-words` | 敏感词列表 | 运营管理员 |
| POST | `/api/v1/admin/sensitive-words` | 添加敏感词 | 运营管理员 |
| PUT | `/api/v1/admin/sensitive-words/{id}` | 编辑敏感词 | 运营管理员 |
| DELETE | `/api/v1/admin/sensitive-words/{id}` | 删除敏感词 | 运营管理员 |
| POST | `/api/v1/admin/sensitive-words/batch` | 批量导入 | 运营管理员 |

#### 数据统计

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/statistics/overview` | 数据概览 | 运营管理员 |
| GET | `/api/v1/admin/statistics/users` | 用户统计 | 运营管理员 |
| GET | `/api/v1/admin/statistics/goods` | 商品统计 | 运营管理员 |
| GET | `/api/v1/admin/statistics/trade` | 交易统计 | 运营管理员 |
| GET | `/api/v1/admin/statistics/report` | 举报统计 | 运营管理员 |

#### 操作日志

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|:--:|
| GET | `/api/v1/admin/logs` | 操作日志列表（筛选+分页） | 超管 |

---

## 九、开发计划

### 9.1 7周迭代计划

| 周次 | 里程碑 | 详细任务 | 验收标准 |
|:--:|------|------|------|
| **W1** | 项目基础设施 | ① Spring Boot 脚手架 ② uni-app 项目初始化 + uView UI ③ 数据库全部表创建 ④ Redis 配置 ⑤ 微信小程序注册+开发者配置 ⑥ 统一异常处理 + 响应格式 | 后端启动无报错；uni-app 可编译至微信开发者工具 + H5 浏览器；所有表创建含索引 |
| **W2** | 用户系统（双端） | ① 手机号+密码注册（双端共用） ② 短信验证码 ③ 微信登录（小程序端 code→openid→token） ④ 小程序端 openid 绑定 ⑤ 个人中心 CRUD ⑥ 账号注销 | 小程序微信授权注册+Web端手机号注册均可用；双端同手机号互通登录 |
| **W3** | 商品模块 | ① 商品分类数据初始化 ② 发布商品（图片上传+压缩，双端 uni-app 兼容） ③ 商品列表（分页+筛选） ④ 商品详情页 | 小程序和 Web 均可发布商品；图片上传正常；列表首屏加载≤2s |
| **W4** | 交易 + 评价 + 搜索 | ① 预订/取消/确认交易 ② 并发预订锁 + 超时释放定时任务 ③ 评价系统（含双盲显示） ④ 搜索功能 ⑤ 收藏 ⑥ 站内通知（+小程序订阅消息） ⑦ 浏览记录 | 预订有并发控制；评价双盲展示；24h超时释放正常；双端均可用 |
| **W5** | 后台管理（Web） | ① 管理员登录+权限 ② 用户管理 ③ 商品管理 ④ 举报处理 ⑤ 评价申诉处理 ⑥ 敏感词管理 ⑦ 数据统计 ⑧ 操作日志 | 角色权限矩阵生效；手机号分级脱敏；操作日志完整 |
| **W6** | 双端适配 + 测试 | ① 小程序端条件编译适配（分享、订阅消息） ② Web 端适配（路由模式、CSRF 防护） ③ 单元测试(≥50%) ④ 集成测试 ⑤ 压力测试(500并发) | 双端功能一致；测试通过 |
| **W7** | 审核 + 灰度 + 上线 | ① 微信小程序提审+审核 ② Web 端部署至 Nginx ③ 灰度发布(体验版→10%→全量) ④ 监控告警 | 微信审核通过；Web 端正常访问；线上可正常使用 |

### 9.2 灰度发布流程

```
W7周一：部署后端生产环境 + 数据库 + Web 静态文件至 Nginx
W7周二：提交小程序至微信审核（首次审核约1-3个工作日）
W7周三：审核通过 → 发布体验版（团队内测）
      ├── 审核被拒 → 根据驳回原因修改（≤1天）→ 重新提交
W7周四：体验版无问题 → 提交正式版审核
W7周五：审核通过 → 全量发布（灰度：先5%用户，观察1h后全量）
W7周末：值班监控 + 紧急修复通道
```

> **微信审核注意事项**：
> - 小程序类目选择"二手闲置"需提供相关资质，建议先以"工具-信息查询"类目上线
> - 首次提交需包含《用户协议》《隐私政策》入口
> - 避免在提审版本中包含未完成功能或测试数据
> - **审核缓冲**：首次审核大概率被拒，预留1-2天修改重提时间

### 9.3 上线后监控

| 监控项 | 指标 | 告警触发 |
|--------|------|---------|
| 接口可用性 | 成功率≥99% | 低于95% |
| 接口响应时间 | P95 ≤ 1s | 超过2s |
| 服务器CPU | < 70% | 超过85% |
| 服务器内存 | < 80% | 超过90% |
| 数据库连接数 | < 50 | 超过80 |
| 注册量 | — | 异常激增（防批量注册） |

---

## 十、测试策略

### 10.1 测试分层

| 层级 | 范围 | 覆盖率目标 | 工具 |
|------|------|:--------:|------|
| 单元测试 | Service层、工具类 | ≥ 50% | JUnit 5 + Mockito |
| 集成测试 | Controller + DB | ≥ 50% | Spring Boot Test + H2 |
| API测试 | 全部接口 | 100% | Postman / REST Assured |
| UI自动化 | 核心流程（微信登录/发布/预订/评价） | 5条主流程 | 微信开发者工具自动化 / minium |
| 压力测试 | 关键API | 500并发 | JMeter |
| 安全测试 | SQL注入/XSS/CSRF/文件上传 | 全量 | 手工 + ZAP |

### 10.2 核心测试场景

1. **注册→发布→预订→交易确认→互评** 全链路
2. 两人同时预订同一商品（并发测试）
3. 预订超时24h自动释放 + 商品回"在售"
4. 多实例部署下超时释放任务不重复执行
5. 敏感词拦截 + 人工审核
6. 图片上传各种异常（过大/非图片/损坏文件）
7. 微信登录异常（code过期、openid获取失败）降级处理
8. 信用分变化全流程（含释放扣分、申诉扣分）
9. 评价双盲展示（一方评完另一方看不到）
10. 评价申诉全流程
11. 买家"联系不上卖家"→自动取消流程
12. 后台管理员各角色权限校验

---

## 十一、风险与应对

### 11.1 业务风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|:--:|:--:|------|
| 用户不回来确认交易 | 高 | 高 | 卖家催确认功能 + 完成交易奖励信用分 + 预订释放惩罚（信用分-3）|
| 虚假商品/骗子 | 中 | 高 | 学号实名 + 信用分 + 举报 + 违规阶梯处罚 |
| 恶意预订占坑 | 中 | 中 | 预订上限3件 + 24h超时释放 + 释放扣信用分 |
| 线下交易纠纷（货不对板） | 中 | 中 | 当面验货提示 + 描述不符举报 + 争议客服介入 |
| 用户量不足 | 高 | 高 | 种子内容 + 班级群推广 + 线下海报 + 口碑传播 |

### 11.2 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|:--:|:--:|------|
| 图片加载慢 | 高 | 中 | 前端压缩 + 缩略图 + Nginx缓存 + 后续CDN |
| 并发预订抢锁 | 中 | 高 | Redis SETNX + DB乐观锁 + 异常回滚 |
| 定时任务多实例重复执行 | 中 | 中 | Redis分布式锁（booking:release-job:lock），仅一个实例执行 |
| 定时任务故障导致预订未释放 | 中 | 中 | 定时任务监控 + 后台手动释放功能 + 超时队列幂等设计 |
| 数据库单点故障 | 低 | 高 | 每日备份 + 云存储异地备份 + 后续主从 |
| 服务器宕机 | 低 | 高 | systemd 自动拉起 jar 包 + 备份快速恢复 |
| 微信API调用失败 | 低 | 中 | 微信登录/手机号授权/订阅消息接口调用异常时，自动降级为站内通知；核心接口添加重试机制 + 监控告警 |

### 11.3 合规风险

| 风险 | 应对 |
|------|------|
| 用户信息收集合规 | 注册时明确告知信息收集范围+用途，勾选同意《用户协议》《隐私政策》 |
| 个人信息保护 | 手机号AES加密存储、管理后台分级脱敏、注销后匿名化处理 |
| 内容合规 | 敏感词过滤、举报机制、管理员24h响应；图片审核V1.5接入 |
| 学校管理规定 | 提前向学校报备、海报合规张贴、不干扰教学秩序 |

---

## 十二、冷启动与运营

### 12.1 冷启动策略

**上线前1周（预热期）：**
- 团队每人发布5-8件真实二手商品（合计30-50件种子内容）
- 覆盖所有一级分类，价格参考闲鱼打8折
- 制作宣传海报（A3 + A4），含二维码 + 核心卖点文案

**上线第1周（引爆期）：**
- **核心传播**：团队成员将小程序分享卡片转发至各班级群，附一句推荐语（如"咱们学校自己的二手交易平台，扫码就能用"）
- 宿舍楼海报：每栋宿舍楼下张贴（提前报备），海报含小程序码，微信扫码直达
- **关键动作**：团队同学现场帮助感兴趣的同学发布第一件商品
- 目标：100注册用户 / 200件在售商品

**上线第2-4周（增长期）：**
- 食堂/图书馆地推：午餐/晚餐时段摆摊（1-2次/周）
- "毕业季大甩卖"专题推广（如逢毕业季）
- 鼓励已注册用户邀请室友（自然增长）
- 目标：500注册用户 / 1000件在售商品

### 12.2 运营日历（示例）

| 时间节点 | 运营主题 | 动作 |
|---------|---------|------|
| 2月底/8月底 | 开学季 | "新学期不花冤枉钱"——教材专题推广 |
| 5月/11月 | 期中季 | "学长学姐的复习资料"——考研/考证资料专题 |
| 6月 | 毕业季 | "带不走的留给学弟学妹"——毕业生大甩卖 |
| 全年 | 日常运营 | 热门商品推荐、违规内容清理、用户反馈响应 |

---

## 十三、附录

### 13.1 敏感词示例

| 分类 | 示例词汇 |
|------|---------|
| 违禁品 | 枪支、弹药、毒品、大麻、管制刀具、假证、假币、窃听器、迷药 |
| 广告引流 | 加微信、加V、免费领取、扫码关注、刷单、兼职、代理、微商 |
| 色情 | 色情、黄色、约炮、裸聊、原味、陪睡 |
| 政治敏感 | （略，参考网信办敏感词库） |
| 违规药品 | 处方药、安眠药、减肥药、增肌药 |
| 赌博 | 彩票、赌球、时时彩、六合彩 |

### 13.2 页面原型（关键页面布局描述）

**首页：**
```
┌──────────────────────────────────┐
│ 🔍 搜索你想找的闲置...    [登录] │
├──────────────────────────────────┤
│ [全部][教材][数码][生活][服装]... │ ← 可左右滑动
├──────────────────────────────────┤
│ ┌──────┐ ┌──────┐              │
│ │      │ │      │  热门推荐     │ ← 横向滑动
│ │ ¥30  │ │ ¥200 │              │
│ └──────┘ └──────┘              │
├──────────────────────────────────┤
│ 筛选: [价格▼] [成色▼] [排序▼]    │
├──────────────────────────────────┤
│ ┌────────┐ ┌────────┐          │
│ │ 图片    │ │ 图片    │          │
│ │ ¥50    │ │ ¥30    │          │ ← 两列瀑布流
│ │ 99新   │ │ 9成新   │          │
│ └────────┘ └────────┘          │
│ ...  加载更多 ...               │
├──────────────────────────────────┤
│  [首页] [发布] [消息] [我的]     │ ← 小程序原生 tabBar
└──────────────────────────────────┘
```

**商品详情页：**
```
┌──────────────────────────────────┐
│ ← 返回           ⋯ [举报]       │
├──────────────────────────────────┤
│ ┌──────────────────────────────┐ │
│ │     商品图片轮播（可缩放）    │ │
│ │     ● ○ ○ ○  第1/4张        │ │
│ └──────────────────────────────┘ │
├──────────────────────────────────┤
│ ¥30           [99新]            │
│ 考研数学复习全书（数一）         │
│ 原价¥78  |  3天前发布           │
├──────────────────────────────────┤
│ 商品描述                        │
│ 去年考研买的，只看了前两章，     │
│ 几乎全新，全书无笔记无折痕。     │
│ 张宇主编，2025版。              │
├──────────────────────────────────┤
│ 卖家信息                        │
│ 🧑 小明  ★98%好评  [信誉良好]   │
│ 在售12件 | 已成交35笔           │
├──────────────────────────────────┤
│ 交易信息                        │
│ 📍 一食堂门口                   │
│ 📱 预订后可查看卖家联系方式      │
├──────────────────────────────────┤
│      [♡ 收藏]    [我要预订]     │
├──────────────────────────────────┤
│ 卖家评价（最近5条）              │
│ ★★★★★ "学长人很好，书也很新"    │
│ ★★★★★ "交易顺利"               │
└──────────────────────────────────┘
```

**订单详情页（预订后）：**
```
┌──────────────────────────────────┐
│ ← 返回          订单状态：已预订  │
├──────────────────────────────────┤
│ 商品信息                        │
│ 考研数学复习全书（数一）  ¥30    │
│ 成色：99新                      │
├──────────────────────────────────┤
│ 卖家信息                        │
│ 昵称：小明  ★98%好评            │
│ 📱 13800138000  [复制] [拨打]   │
│ 💬 微信号：xiaoming_123 [复制]   │
├──────────────────────────────────┤
│ 买家信息（卖家可见）             │
│ 📱 13900139000  [复制] [拨打]   │
├──────────────────────────────────┤
│ ⚠️ 请通过微信/电话联系卖家约定   │
│    交易时间和地点，当面验货付款   │
│                                 │
│ [取消预订] [联系不上] [确认交易] │
└──────────────────────────────────┘
```

### 13.3 数据字典（关键枚举值）

**商品成色：**
| 值 | 含义 |
|:--|------|
| 1 | 全新未拆 |
| 2 | 99新 |
| 3 | 9成新 |
| 4 | 8成新 |
| 5 | 战损 |
| 6 | 故障品 |

**商品状态：**
| 值 | 含义 |
|:--|------|
| 0 | 已下架 |
| 1 | 在售 |
| 2 | 已预订 |
| 3 | 已售出 |

**订单状态：**
| 值 | 含义 |
|:--|------|
| 1 | 已预订 |
| 2 | 交易完成 |
| 3 | 已取消 |
| 4 | 已过期（超时系统自动释放） |

**用户状态：**
| 值 | 含义 |
|:--|------|
| 0 | 禁用 |
| 1 | 正常 |
| 2 | 注销 |

**评价类型：**
| 值 | 含义 |
|:--|------|
| 1 | 好评 |
| 2 | 中评 |
| 3 | 差评 |

**评价申诉状态：**
| 值 | 含义 |
|:--|------|
| 0 | 正常 |
| 1 | 申诉中 |
| 2 | 申诉通过（隐藏评价） |
| 3 | 申诉驳回 |

**管理员角色：**
| 值 | 含义 |
|:--|------|
| 1 | 超级管理员 |
| 2 | 运营管理员 |
| 3 | 客服管理员 |
| 4 | 内容审核员 |

**举报状态：**
| 值 | 含义 |
|:--|------|
| 0 | 待处理 |
| 1 | 通过（处罚） |
| 2 | 驳回 |

**通知类型：**
| 值 | 含义 |
|:--|------|
| 1 | 预订通知 |
| 2 | 释放通知 |
| 3 | 催确认通知 |
| 4 | 联系不上通知 |
| 5 | 评价通知 |
| 6 | 系统通知 |
| 7 | 举报通知 |
| 8 | 处罚通知 |
| 9 | 评价到期提醒 |

**短信发送类型：**
| 值 | 含义 |
|:--|------|
| 1 | 注册（H5备用） |
| 2 | 登录（H5备用） |
| 3 | 修改密码（H5备用） |
| 4 | 其他 |

---

> **文档变更记录：**
> - v1.0 (2026-05-20)：初版MVP功能设计
> - v2.0 (2026-05-22)：补充交易闭环、敏感词审核、权限分级、量化标准
> - v3.0 (2026-06-07)：重构升级——新增数据库交易订单表、信用分体系、消息通知；改进预订并发控制、评价双盲机制、联系方式分级展示、灰度发布方案、测试策略、完整API规范
> - v4.0 (2026-06-07)：修复v3.0的11项问题——①学校名称全局统一为齐鲁工业大学 ②删除矛盾的72h自动确认逻辑，统一24h释放+卖家催确认 ③测试覆盖率统一70% ④补全user_notification/user_browse_history/sms_log三张表DDL ⑤联系方式改为预订后完整展示便于线下联络 ⑥订单状态流程图补全"已过期"状态 ⑦新增后台管理API完整接口定义 ⑧评价恶意申诉处罚闭环 ⑨图片审核标注为V1.5功能 ⑩联系方式增加快照机制和AES加密说明 ⑪新增预订超时队列Redis方案、定时任务监控等细节
> - v5.0 (2026-06-07)：修复v4.0的4项问题——①原型图联系方式脱敏→完整号码（与§4.1.5一致） ②多实例部署定时任务竞争→新增释放任务分布式锁booking:release-job:lock ③缺少"联系不上卖家"接口→新增POST /api/v1/trade/{orderId}/contact-fail ④缺少批量已读接口→新增PUT /api/v1/notification/read-all；此外补全了sys_admin/goods_category/goods_image/user_favorite/report_record/sensitive_word/admin_log七张次要表的DDL，新增后台手动释放预订接口，数据字典扩充枚举值
> - v6.0 (2026-06-07)：修复v5.0的2处遗漏——①§3.3状态表"已预订"操作列补充"卖家取消预订" ②trade_order表新增contact_fail_at字段支撑"联系不上卖家"的24h倒计时
> - v7.0 (2026-06-07)：产品形态切换为微信小程序——①前端技术栈Vue3+VantUI→uni-app(Vue3)+uView UI ②注册登录流程简化为微信授权+手机号快速验证（免密码/免短信验证码） ③sys_user表移除password字段新增openid/unionid ④部署方案调整为小程序无需前端部署 ⑤新增微信订阅消息通知 ⑥图片上传改为直传OSS ⑦新增小程序审核流程 ⑧安全方案去掉CSRF(小程序天然免疫)
> - v8.0 (2026-06-07)：修复v7.0的3处矛盾+7处优化——①§4.2.4图片处理流程改为OSS直传 ②§4.1.4注销确认改为微信授权验证 ③§10.2移除短信防刷测试→替换为微信登录异常测试 ④§11.2短信风险→微信API调用失败风险 ⑤§1.2差异化补充小程序独占优势 ⑥§12.1冷启动强调小程序分享卡片传播 ⑦§4.2.6搜索标注V1.0用MySQL LIKE ⑧§9.2审核流程补充被拒缓冲时间 ⑨sms_log标注H5备用 ⑩管理后台openid不导出仅超管可查看
> - v9.0 (2026-06-07)：新增Web端，uni-app双端编译——①产品形态改为小程序+Web双端 ②sys_user表加回password字段，openid改为可选唯一，phone为双端唯一标识 ③注册登录支持两套入口（小程序微信授权+Web手机号密码） ④图片存储回归本地（双端统一上传至后端） ⑤技术栈明确条件编译策略（#ifdef MP-WEIXIN/#ifdef H5） ⑥部署方案增加H5静态文件托管 ⑦开发计划延长至7周（增加双端适配周） ⑧安全方案补充Web端CSRF防护 ⑨新增wx-bind接口支持Web端绑定微信
> - v10.0 (2026-06-07)：修复v9.0的6处v8→v9转换遗留——①§8.1文件上传"OSS直传"→"multipart上传至后端" ②§8.1 Token描述补充Web端签发来源 ③§9.1标题"6周"→"7周" ④sys_user openid COMMENT去掉"唯一"(Web端为NULL) ⑤sms_log COMMENT更新为"双端注册登录使用" ⑥§9.2灰度发布时间线W6→W7 ⑦§11.2服务器宕机"Docker"→"systemd"
> - v11.0 (2026-07-13)：基于审查报告v1.0修复27项问题——**P0级**：①§6.3联系方式加密改为HMAC盲索引+AES-GCM随机IV(消除ECB安全漏洞) ②sys_user表新增phone_hash字段+cancel_status/cancel_at字段 ③trade_order表新增seller_qq快照字段 ④新增credit_log信用分变更日志表 ⑤取消预订区分买卖双方(卖家取消不扣买家信用分) ⑥contact-fail接口标注幂等约束。**P1级**：⑦新增卖家"已联系买家"响应API(seller-respond) ⑧goods_info新增last_relisted_at字段(30天下架按最近上架时间计算) ⑨商品列表/详情/搜索接口标注"可选认证" ⑩session_key TTL从30天改为1天 ⑪联系不上流程补充幂等说明 ⑫通知保留30天补充清理机制说明。**P2级**：⑬goods_condition注释格式修复 ⑭sms_log注释修正为"双端注册时使用" ⑮学号白名单标注V1.5实现 ⑯Web端登录连续3次失败强制图形验证码。**P3级**：⑰单元测试覆盖率目标从70%降至50%(MVP合理)
